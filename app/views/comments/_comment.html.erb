
    <div>
      <%= image_tag comment.user.get_profile_image(50,50) %>
      <%= comment.body %>
      <%= comment.user.name %>
      <%= comment.created_at.strftime('%Y/%m/%d %H:%M:%S') %>
    </div>
