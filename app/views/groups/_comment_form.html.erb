  <%= form_with model: [group, comment],local: false do |f| %>
    <%= f.text_area :body, id: "comment_content", rows: '5', placeholder: "コメントをここに" %>
    <%= f.submit "送信する", id: 'comment-button' %>
  <% end %>